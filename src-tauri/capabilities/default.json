{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Deliberately minimal: a file-save dialog for exports, revealing the data folder, and opening the release page in the user's browser. All network access happens in Rust, so the webview needs no network permissions at all.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-save",
    "opener:allow-reveal-item-in-dir",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://github.com/MarkoVcode/*"
        }
      ]
    }
  ]
}
