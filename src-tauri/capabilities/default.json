{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Deliberately minimal: the app needs a file-save dialog for exports and the ability to reveal the data folder. All network access happens in Rust, so the webview needs no network permissions at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-save",
    "opener:allow-reveal-item-in-dir"
  ]
}
